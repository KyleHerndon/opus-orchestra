/**
 * VSCode-specific configuration constants
 *
 * ExtensionConfig and DEFAULT_CONFIG are now in @opus-orchestra/core
 */

/**
 * VS Code configuration section name
 */
export const CONFIG_SECTION = 'claudeAgents';
